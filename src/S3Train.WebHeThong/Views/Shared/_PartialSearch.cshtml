@using (Html.BeginForm())
{
    <div class="row">
        @Html.TextBox("searchString", "", new { @class = "form-control col-md-9", @placeholder = "Tên tủ hoặc tên người quản lý" })
        &ensp;
        <input type="submit" value="Tìm Kiếm" class="btn btn-primary col-md-auto" />
    </div>
}